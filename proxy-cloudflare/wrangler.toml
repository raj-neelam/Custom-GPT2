name = "gpt2-proxy"
main = "src/index.js"
compatibility_date = "2024-01-01"

# Uncomment to bind a custom domain / subdomain:
# routes = [
#   { pattern = "api.yourdomain.com", zone_name = "yourdomain.com" }
# ]

# Secrets (DO NOT put real token here — use: wrangler secret put HF_TOKEN)
# HF_TOKEN is injected at runtime from Cloudflare's secret store.
